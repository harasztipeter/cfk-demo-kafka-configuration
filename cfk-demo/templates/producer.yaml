apiVersion: v1
kind: Pod
metadata:
  name: producer
  namespace: {{ $.Values.namespace }}
spec:
  restartPolicy: Never
  containers:
    - name: producer
      image: producer:latest
      imagePullPolicy: Never
      command: ["python3", "-u", "./producer.py"]