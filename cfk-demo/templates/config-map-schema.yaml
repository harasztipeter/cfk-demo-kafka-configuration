{{- range $schema := .Values.schemas }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-config" $schema.name }}
  namespace: {{ $.Values.namespace }}
data:
  schema: {{ printf "schemas/%s.json" $schema.name | $.Files.Get | quote}}
---
{{- end }}