{{- range $schema := .Values.schemas }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-config" $schema.name }}
  namespace: {{ $.Values.namespace }}
data:
  schema: {{ printf "%s" $schema.file | $.Files.Get | indent 4}}
---
{{- end }}